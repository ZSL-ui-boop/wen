# Free-FS 核心功能实现笔记
---

## 目录

1. [大文件上传](#1-大文件上传)
2. [可视化数据大屏](#2-可视化数据大屏)
3. [AI 对话](#3-ai-对话)
4. [Agent 智能助手](#4-agent-智能助手)
5. [CI/CD 流水线](#5-cicd-流水线)

---

## 1. 大文件上传

### 1.1 核心技术栈

| 技术 | 是什么 | 为什么用 | 如何使用 |
|------|--------|----------|----------|
| **分片上传** | 将大文件切成小块逐块上传 | 绕过 HTTP 单次传输大小限制；支持断点续传 | 前端 5MB 一片切割，后端接收后缓存 ETag，全部收齐后合并 |
| **SparkMD5 + 快速指纹** | 浏览器端 MD5 计算库 | 秒传检测需要文件摘要；全量计算大文件会卡死 UI | ≥100MB 使用头/中/尾采样指纹，<100MB 全量计算，每 2MB yield 让出主线程 |
| **Zustand** | 轻量 React 状态管理 | 管理上传任务全局状态（进度、状态机、会话分组） | `create()` + Map 存储多任务状态，`transitionTo` 驱动状态机流转 |
| **SSE (Server-Sent Events)** | 服务端单向推送协议 | 实时推送上传进度，比 WebSocket 轻量 | 后端 SseEmitter，前端 `ReadableStream` 解析，断连时降级 3s 轮询 |
| **指数退避重试** | 失败后延迟递增重试 | 避免网络抖动导致上传失败 | 分片失败后 1s→2s→4s 递增重试，最多 3 次，网络错误自动暂停 |
| **状态机** | 状态转换规则表 | 确保任务状态流转有序、可追溯 | `VALID_TRANSITIONS` Map 定义转换规则，非法转换返回 null |
| **Redis 缓存** | 内存 KV 缓存 | 分片上传过程中缓存已传分片 ETag，支持断点续传 | 每次分片成功后写入 Redis，查询已传分片时从 Redis 读取 |
| **存储插件化** | 抽象存储层接口 | 支持多种存储后端（本地/S3/OSS/Kodo/OBS） | `IStorageOperationService` 接口，不同存储分别实现 multipart upload |

### 1.2 核心实现流程

```
用户选择文件 / 拖拽文件夹
    │
    ▼
UploadModal 弹窗
    ├── 文件模式：最多 10 个文件
    └── 目录模式：解析 webkitRelativePath 目录树，递归创建文件夹
    │
    ▼
startUploadSession() ── 创建 sessionId，分组管理
    │
    ▼
createTask(file) ── 循环每个文件
    │
    ├── 读取用户传输设置（分片大小、并发数）
    ├── 注册 uploadExecutor 回调（全局仅一次）
    ├── POST /apis/transfer/init ── 服务端创建 FileTransferTask，返回 taskId
    └── 写入 store + fileCache
    │
    ▼
uploadExecutor.start(taskId, file)
    │
    ├── 1. notifyTransition('checking')
    ├── 2. calculateFileMD5(file)
    │       ├── < 100MB → 完整 MD5
    │       └── ≥ 100MB → 头/中/尾各 2MB 采样指纹
    │
    ├── 3. POST /apis/transfer/check
    │       ├── isQuickUpload=true ──▶ 秒传命中，直接 complete
    │       └── isQuickUpload=false ──▶ 继续上传
    │
    ├── 4. GET /apis/transfer/chunks/{taskId} ── 断点续传查询已传分片
    │
    ├── 5. notifyTransition('uploading')
    │
    ├── 6. uploadChunks(context) ── 并发 worker 池
    │       ├── N = min(concurrency, 待传分片数)
    │       ├── 每个 worker 循环取分片：
    │       │     ├── file.slice(start, end) 切片
    │       │     ├── calculateBlobMD5(chunk)
    │       │     ├── POST /apis/transfer/chunk (multipart, 60s 超时)
    │       │     ├── 成功 → uploadedChunks.add(index)，通知进度
    │       │     └── 失败 → 指数退避重试（最多 3次）
    │       └── Promise.all(workers)
    │
    ├── 7. 全部分片完成 → notifyTransition('merging')
    │
    └── 8. SSE 推送 complete → 前端 transitionTo('completed')
            ├── toast 通知
            ├── dispatchEvent('file-upload-complete')
            ├── 清理 progressCalculator + fileCache
            └── UploadPanel 3秒后自动收起
```

### 1.3 关键代码文件

| 文件 | 行数 | 核心职责 |
|------|------|----------|
| `src/services/upload-executor.ts` | ~517 行 | 上传执行器单例：MD5 计算、分片调度、暂停/恢复/取消、指数退避重试 |
| `src/store/transfer.ts` | ~1097 行 | 全局状态管理：任务生命周期、SSE 连接、批量上传、轮询降级 |
| `src/api/transfer.ts` | 126 行 | REST API 封装：init/check/chunk/pause/resume/cancel |
| `src/utils/md5.ts` | ~150 行 | 全量 MD5 + 快速指纹（≥100MB采样） |
| `src/utils/progress-calculator.ts` | 218 行 | 滑动窗口测速 + 节流更新 |
| `src/utils/transfer-state-machine.ts` | 70 行 | 状态转换规则定义 |
| `src/utils/async-pool.ts` | ~40 行 | 限流并发工具 |
| `src/config/upload-limits.ts` | ~80 行 | 上传限制：文件数/总大小/目录深度/文件名长度 |

### 1.4 核心代码片段

**上传执行器主流程（upload-executor.ts）：**

```typescript
/**
 * 上传执行器主入口
 * @param taskId   服务端 init 接口返回的任务 ID
 * @param file     待上传的原始 File 对象
 * @param concurrency  并发 worker 数，默认 3
 * @param chunkSize    分片大小，默认 5MB
 */
public async start(taskId, file, concurrency?, chunkSize?) {
  // 创建任务上下文：分片数、已传集合、暂停/取消标志等
  const context = this.createContext(taskId, file, chunkSize, concurrency)
  this.taskContexts.set(taskId, context) // 注册到内存 Map，供暂停/恢复/取消查询

  // 1. 进入 checking 状态，开始计算文件 MD5（≥100MB 用采样指纹）
  this.notifyTransition(taskId, 'checking')
  const fileMd5 = await calculateFileMD5(file)

  // 2. 秒传检测：后端比对 MD5，命中则直接完成，无需上传
  const checkResult = await checkUpload({ taskId, fileMd5, fileName: file.name })
  if (checkResult.isQuickUpload) {
    this.notifyTransition(taskId, 'completed')
    return // 秒传命中，流程结束
  }

  // 3. 断点续传：从 Redis 拉取已传分片索引，跳过已完成部分
  const uploadedChunks = await getUploadedChunks(taskId)
  uploadedChunks.forEach(i => context.uploadedChunks.add(i))

  // 4. 进入 uploading 状态，启动 worker 池并发上传剩余分片
  this.notifyTransition(taskId, 'uploading')
  await this.uploadChunks(context)

  // 5. 全部分片上传完成 → merging（实际合并由服务端自动触发）
  if (context.uploadedChunks.size === totalChunks) {
    this.notifyTransition(taskId, 'merging')
  }
}
```

**并发 worker 池（upload-executor.ts）：**

```typescript
/**
 * 并发上传所有未传分片（worker 池模式）
 * N 个 worker 共享一个游标，从待传队列取分片索引依次上传
 */
private async uploadChunks(context: UploadTaskContext) {
  const { taskId, totalChunks, uploadedChunks, concurrency } = context

  // 收集尚未上传的分片索引（断点续传后可能只剩部分）
  const chunksToUpload: number[] = []
  for (let i = 0; i < totalChunks; i++) {
    if (!uploadedChunks.has(i)) chunksToUpload.push(i)
  }
  if (chunksToUpload.length === 0) return // 全部已传，无需上传

  let currentIndex = 0 // 共享游标，JS 单线程递增无竞态

  const worker = async () => {
    while (currentIndex < chunksToUpload.length) {
      // 暂停/取消时退出循环，当前分片传完后不再取新任务
      if (context.isPaused || context.isCancelled) break

      const chunkIndex = chunksToUpload[currentIndex++] // 取下一个待传分片
      const result = await this.uploadChunkWithRetry(context, chunkIndex)

      if (result.success) {
        uploadedChunks.add(chunkIndex)       // 记录已传分片
        this.notifyProgress(taskId, context) // 通知 store 更新进度条
      } else if (!context.isCancelled && !context.isPaused) {
        throw new Error(result.error) // 非用户中断的失败，终止所有 worker
      }
    }
  }

  // 启动 N 个 worker，N = min(并发数, 待传分片数)
  const count = Math.min(concurrency, chunksToUpload.length)
  await Promise.all(Array.from({ length: count }, () => worker()))
}
```

**状态机定义（transfer-state-machine.ts）：**

```typescript
/**
 * 上传任务状态机：定义各状态允许转换到的下一状态
 * key = 当前状态，value = 合法目标状态列表
 * 非法转换时 transition() 返回 null，防止状态回退或跳跃
 */
export const VALID_TRANSITIONS = {
  idle: ['initialized'],                              // 空闲 → 已初始化（创建任务）
  initialized: ['checking', 'failed', 'cancelled'],   // 初始化 → 开始校验 / 失败 / 取消
  checking: ['uploading', 'completed', 'failed', 'cancelled'], // 校验中 → 上传 / 秒传完成 / 失败 / 取消
  uploading: ['paused', 'merging', 'failed', 'cancelled'],    // 上传中 → 暂停 / 合并 / 失败 / 取消
  paused: ['uploading', 'cancelled'],                 // 暂停 → 恢复上传 / 取消
  merging: ['completed', 'failed'],                   // 合并中 → 完成 / 失败
  completed: [],                                      // 终态，不可再转换
  failed: ['initialized'],                            // 失败后可重试（回到 initialized 重新上传）
  cancelled: [],                                      // 终态，不可再转换
}
```

### 1.5 重难点分析

| 问题 | 解决方案 |
|------|----------|
| **大文件 MD5 计算卡死 UI** | ≥100MB 文件采用采样指纹（头/中/尾各 2MB），每读取 2MB 调用 `yieldToMain()` 让出主线程 |
| **网络波动导致进度回退** | 进度计算器内部 `lastProgress` 只增不减，网络抖动也不会让进度条倒退 |
| **进度更新太频繁导致 UI 抖动** | `ProgressCalculator` 内置 100ms 节流，5s 滑动窗口测速，平滑剩余时间 |
| **页面刷新中断未完成任务** | `checkUnfinishedTasks()` 在初始化时自动取消上次未完成的任务并提示用户 |
| **SSE 断连** | 降级为 3s 轮询 `syncTasks()`，优先级算法避免 SSE 与轮询互相覆盖 |
| **暂停/恢复状态不一致** | 先暂停前端 worker，再调后端 API，后端失败时回滚前端状态 |
| **文件夹批量上传结构复杂** | 解析 `webkitRelativePath`，排序确保父目录先创建，限流 `pool(8)` 并发创建任务 |
| **分片上传中断（浏览器关闭）** | `beforeunload` 事件拦截，提示用户有上传中的任务 |
| **高并发上传打满连接** | 单文件 concurrency=3，批量创建 pool=8，避免同时发起过多请求 |

### 1.6 项目答辩话术

> 大文件上传是我们项目的核心亮点。我们采用了经典的分片上传方案，默认每片 5MB，支持 3 个分片并发上传。在上传前，前端会计算文件的 MD5，送到后端做秒传检测——如果服务端已经有相同文件，就直接返回完成，实现"秒传"。如果没命中，再通过查询已传分片来实现断点续传。
>
> 大文件的 MD5 计算是个挑战——超过 100MB 的文件如果全量计算，浏览器会卡死。我们的方案是采样计算，只取文件头、中、尾各 2MB 来生成指纹，再结合文件大小和时间戳做二次哈希，既保证了精度，又大幅提升了速度，期间还通过 requestIdleCallback 定期让出主线程。
>
> 进度反馈方面，我们使用 SSE 服务端推送，后端每收到一个分片就推送进度事件。如果 SSE 断连，自动降级为 3 秒轮询。前端还有一个滑动窗口测速器，用最近 5 秒的数据计算实时速度和剩余时间，并且进度只增不减，保证用户体验平滑。
>
> 我们还实现了完整的任务状态机——从 idle 到 initialized、checking、uploading、merging、completed，以及暂停、失败、取消等分支状态，每个状态转换都经过合法性校验，确保数据一致性。

---

## 2. 可视化数据大屏

### 2.1 核心技术栈

| 技术 | 是什么 | 为什么用 | 如何使用 |
|------|--------|----------|----------|
| **Recharts 2** | React 原生图表库 | 相比 ECharts 更轻量、React 友好、声明式 API | `<AreaChart>` 面积图展示存储增长趋势 |
| **TanStack Query** | React 数据请求缓存库 | 自动管理 loading/error/refetch 状态 | 指标 30s、图表/概览 60s 分路轮询，组件卸载自动停止 |
| **HTTP 轮询** | 定时 REST 请求 | 大屏为秒级刷新，无需 WebSocket 双向通道 | 未使用 WebSocket/SSE，纯 GET + `refetchInterval` |
| **Tailwind CSS** | 原子化 CSS 框架 | 快速实现深色赛博朋克风格 | 自定义渐变、网格背景、发光效果和科技感配色 |
| **React 19** | UI 框架 | 组件化构建大屏 | 函数组件 + hooks 管理页面状态 |
| **Spring Boot 4** | 后端框架 | 聚合多个数据源统计指标 | `FileHomeServiceImpl.getDashboard()` 查询各表统计 |

### 2.2 核心实现流程

```
用户访问 /w/:slug/big-screen
    │
    ├── 1. 激活工作空间（activateWorkspace，加载角色权限）
    │       └── ready=true 后才允许后续 useQuery 发起请求
    │
    ├── 2. 三路 HTTP 轮询（非 WebSocket / 非 SSE）
    │       │
    │       ├── [30s] 指标卡片 → GET /apis/home/dashboard
    │       │       ├── 成功 → 6 项指标 + trend7d（后端已聚合）
    │       │       └── 404 → 降级 /apis/home/info，计数置 0
    │       │
    │       ├── [60s] 趋势图 ChartStorageGrowth → GET /apis/home/info
    │       │       └── 复用首页组件，独立 queryKey，未用 stats.trend7d
    │       │
    │       └── [60s] 存储概览 StorageOverviewCard → GET /apis/home/info
    │
    ├── 3. 渲染 6 个 MetricCard
    │       ├── 已用存储 (usedStorage + unitLabel)
    │       ├── 文件总数 (fileCount)
    │       ├── 文件夹总数 (folderCount)
    │       ├── 分享总数 (shareCount)
    │       ├── 回收站文件 (recycleCount)
    │       └── 今日上传 (uploadTodayCount)
    │
    ├── 4. 渲染图表区域
    │       ├── ChartStorageGrowth（Recharts AreaChart，variant='bigscreen'）
    │       └── StorageOverviewCard（存储概览，variant='bigscreen'）
    │
    ├── 5. 手动刷新
    │       ├── refetchStats() 刷新指标
    │       └── invalidateQueries(['homeInfo']) 同步刷新图表/概览
    │
    └── 6. 深色科技风 UI
            ├── bg-[#020617] 深蓝黑背景
            ├── 辐射渐变 + 网格线装饰
            ├── cyan 青色主题 + 发光效果（bigscreen-theme.ts 常量）
            └── 指标卡片带 blur 毛玻璃
```

### 2.3 关键代码文件

| 文件 | 核心职责 |
|------|----------|
| `src/pages/big-screen/index.tsx` | 工作空间激活、30s 指标轮询、6 指标卡片、手动刷新 |
| `src/pages/big-screen/bigscreen-theme.ts` | 深色科技风样式常量 |
| `src/api/dashboard.ts` | 仪表盘请求 + 404 降级 |
| `src/pages/home/components/chart-storage-growth.tsx` | 大屏趋势图封装（转发 ChartAreaInteractive） |
| `src/pages/home/components/storage-usage-card.tsx` | 存储概览卡片，60s 独立轮询 |
| `src/components/chart-area-interactive.tsx` | Recharts 面积图 + 大屏 variant 样式切换 |
| `FileHomeServiceImpl.java` | 后端 dashboard 多表聚合统计 |

### 2.4 核心代码片段

**工作空间激活 gate（big-screen/index.tsx）：**

```typescript
// 进入大屏前激活工作空间，后端统计依赖 workspaceId / 角色上下文
React.useEffect(() => {
  if (isLoading || !isAuthenticated || !slug) return
  const workspace = findBySlug(slug)
  if (!workspace) { setReady(true); return }

  const { currentWorkspaceId, currentRole } = useWorkspaceStore.getState()
  if (workspace.id === currentWorkspaceId && currentRole) {
    setReady(true)
    return
  }
  void activateWorkspace(workspace.id).then(() => setReady(true))
}, [slug, isLoading, isAuthenticated, workspaces, activateWorkspace])
```

**指标卡片：30 秒轮询 + 404 降级（big-screen/index.tsx + dashboard.ts）：**

```typescript
// 指标数据：30 秒自动轮询，工作空间就绪后才请求
const { data: stats, refetch: refetchStats } = useQuery({
  queryKey: ['dashboard', slug, storageUnit],
  queryFn: () => getDashboard({ unit: storageUnit }),
  enabled: ready,
  refetchInterval: 30_000,
})

/**
 * 仪表盘数据请求 + 404 降级
 * 新版后端有 /dashboard 接口；旧版返回 404 时降级到 /home/info
 */
export function getDashboard(params?: { unit?: HomeUsedBytesUnit }) {
  return request
    .get<FileDashboard>('/apis/home/dashboard', {
      params,
      showErrorMessage: false, // 404 降级时不弹错误 toast
    })
    .catch(async (err: { response?: { status?: number }; code?: number }) => {
      const is404 = err?.response?.status === 404 || err?.code === 404
      if (!is404) throw err

      const home = await getHomeInfo({ unit: params?.unit ?? 3, dateType: 2 })
      return {
        usedStorage: home.usedStorage,
        unit: home.unit,
        fileCount: 0,
        folderCount: 0,
        shareCount: 0,
        recycleCount: 0,
        uploadTodayCount: 0,
        trend7d: home.usedBytes ?? [],
        updatedAt: new Date().toLocaleString(),
      } satisfies FileDashboard
    })
}
```

**图表 / 概览：60 秒独立轮询（chart-area-interactive.tsx + storage-usage-card.tsx）：**

```typescript
// 趋势图：复用首页组件，独立 queryKey，未使用 stats.trend7d
const { data: home } = useQuery({
  queryKey: ['homeInfo', 'chart', timeRange, unit],
  queryFn: () =>
    getHomeInfo({ unit, dateType: timeRangeToDateType(timeRange) }),
  staleTime: 0, // 切换单位/时间范围后立即重新请求
  refetchInterval: HOME_INFO_REFETCH_INTERVAL_MS, // 60_000
})

// 存储概览卡片：同样独立请求 home 接口
const { data } = useQuery({
  queryKey: ['homeInfo', 'summary', slug, storageUnit],
  queryFn: () => getHomeInfo({ unit: storageUnit }),
  refetchInterval: HOME_INFO_REFETCH_INTERVAL_MS, // 60_000
})
```

**手动刷新（big-screen/index.tsx）：**

```typescript
const handleRefresh = () => {
  void refetchStats() // 刷新 6 项指标
  void queryClient.invalidateQueries({ queryKey: ['homeInfo'] }) // 同步刷新图表/概览
}
```

**后端 dashboard 多表聚合（FileHomeServiceImpl.java）：**

```java
@Override
public FileDashboardVO getDashboard(FileHomeUsedBytesQry qry) {
    String workspaceId = WorkspaceContext.getWorkspaceId();
    String storageId = StoragePlatformContextHolder.getConfigId();
    Integer unit = qry.getUnit() != null ? qry.getUnit() : 2;

    FileDashboardVO vo = new FileDashboardVO();
    vo.setUsedStorage(formatValue(fileInfoService.calculateUsedStorage(), unit));
    vo.setUnit(unitLabel(unit));
    vo.setFileCount(countFiles(workspaceId, storageId, false, false));   // 文件数
    vo.setFolderCount(countFiles(workspaceId, storageId, true, false));  // 文件夹数
    vo.setRecycleCount(countFiles(workspaceId, storageId, null, true)); // 回收站
    vo.setShareCount(fileShareService.count(
        QueryWrapper.create().where(FILE_SHARE.WORKSPACE_ID.eq(workspaceId))));
    vo.setUploadTodayCount(countUploadToday(workspaceId, storageId));    // 今日上传
    vo.setTrend7d(getFileHomeUsedBytes(trendQry));                      // 7 日趋势
    vo.setUpdatedAt(LocalDateTime.now().format(DateTimeFormatter.ofPattern("yyyy-MM-dd HH:mm:ss")));
    return vo;
}
```

**Recharts 面积图 + 大屏主题（chart-area-interactive.tsx + bigscreen-theme.ts）：**

```typescript
// 数据转换
const chartData = home?.usedBytes?.map(row => ({
  date: row.date,
  used: Number(row.usedBytes),
})) ?? []

// 大屏 variant 切换 Recharts 样式
const isBigScreen = variant === 'bigscreen'
const chartConfig = {
  used: { label: '...', color: isBigScreen ? '#22d3ee' : 'var(--chart-1)' },
}

<AreaChart data={chartData}>
  <defs>
    <linearGradient id={fillGradientId} x1='0' y1='0' x2='0' y2='1'>
      <stop offset='5%' stopColor='var(--color-used)' stopOpacity={isBigScreen ? 0.55 : 0.9} />
      <stop offset='95%' stopColor='var(--color-used)' stopOpacity={isBigScreen ? 0.02 : 0.08} />
    </linearGradient>
  </defs>
  <Area type='monotone' dataKey='used' stroke='var(--color-used)' fill={`url(#${fillGradientId})`} />
</AreaChart>

// 主题常量复用
export const bigscreenPanelClass =
  '!rounded-2xl !border !border-cyan-500/20 !bg-slate-950/75 !text-cyan-50 ... !backdrop-blur-xl'
export const bigscreenGlowTop = '... bg-cyan-500/10 blur-3xl'
export const bigscreenGlowBottom = '... bg-violet-500/10 blur-3xl'
```

### 2.5 重难点分析

| 问题 | 解决方案 |
|------|----------|
| **为何不用 WebSocket** | 大屏为秒级单向展示，HTTP 轮询实现简单、兼容性好；WebSocket 适合高频双向通信（聊天、协作） |
| **三路请求如何协调** | 指标 30s / 图表概览 60s 分路轮询，queryKey 隔离缓存；手动刷新时 `invalidateQueries(['homeInfo'])` 统一触发 |
| **dashboard 返回 trend7d 但图表未用** | 图表复用首页 `ChartAreaInteractive`，自带 `getHomeInfo` 请求与时间范围切换，避免与指标轮询耦合 |
| **后端接口版本兼容** | dashboard 接口返回 404 时自动降级到 home 接口，保证旧版后端可用 |
| **大屏路由权限** | 进入大屏前检查工作空间激活状态，未激活的先调用 `activateWorkspace` |
| **深色主题一致性** | 抽取 `bigscreenPanelClass`、`bigscreenGlowTop` 等常量，`variant='bigscreen'` 统一切换 |
| **图表响应式** | 使用 min-h-[360px] + grid 布局，大屏 lg 切换为两列 |

### 2.6 项目答辩话术

> 可视化数据大屏是一个全屏的运营监控页面，展示网盘的核心统计数据。我们使用 Recharts 面积图展示存储增长趋势，配合 6 个指标卡片展示文件数、分享数、回收站等关键数据。
>
> 数据刷新采用 **HTTP 轮询**，没有使用 WebSocket 或 SSE。页面实际上有三路独立请求：指标卡片每 30 秒请求 `/apis/home/dashboard`；趋势图和存储概览复用首页组件，各自每 60 秒请求 `/apis/home/info`。后端 dashboard 接口从多个表中聚合文件数、文件夹数、分享数、今日上传数等指标。我们还兼容了旧版本后端——如果 dashboard 接口不存在，自动降级到首页接口。
>
> 进入大屏前会先激活工作空间，确保后端能拿到正确的 workspaceId 和角色上下文。视觉上采用深色赛博朋克风格，通过 `bigscreen-theme.ts` 统一管理青色主题、毛玻璃和光晕效果，适合投放到办公室大屏展示。

---

## 3. AI 对话

### 3.1 核心技术栈

| 技术 | 是什么 | 为什么用 | 如何使用 |
|------|--------|----------|----------|
| **SSE 流式** | 服务端向客户端单向推送 | LLM 输出是逐步生成的，流式输出用户体验更好 | 后端 SseEmitter 逐行转发 OpenAI delta，前端 `ReadableStream` 逐块读取 |
| **OpenAI 兼容 API** | LLM 标准接口 | 可接入任何兼容的模型（GPT、Claude、DeepSeek 等） | 后端代理请求 `/chat/completions`，stream=true |
| **Web Speech API** | 浏览器原生语音能力 | 免额外 SDK 实现语音输入/播报 | `SpeechRecognition` 听写，`SpeechSynthesis` 播报 |
| **localStorage** | 浏览器本地存储 | 轻量持久化多会话历史，无需后端存储 | `ai-chat-storage.ts` 管理会话创建/切换/删除/持久化 |
| **Java HttpClient + 虚拟线程** | JDK 21 新特性 | 处理 SSE 流式转发需要大量 IO 线程 | `Executors.newVirtualThreadPerTaskExecutor()` 为每个请求创建虚拟线程 |

### 3.1.1 当前模型配置（DeepSeek）

大模型通过 `AiProperties` 统一配置，写在 `application-local.yml`（已 gitignore）或环境变量中。**普通对话、深度思考、Agent 三种模式共用同一模型**，切换厂商只改配置，无需改业务代码。

| 配置项 | 代码默认值 | 当前部署 |
|--------|-----------|----------|
| `ai.enabled` | `false` | `true` |
| `ai.base-url` | `https://api.openai.com/v1` | `https://api.deepseek.com/v1` |
| `ai.model` | `gpt-4o-mini` | `deepseek-chat` |
| `ai.api-key` | 空 | 写在 `application-local.yml`，不提交 Git |

```yaml
# fs-admin/src/main/resources/application-local.yml
ai:
  enabled: true
  base-url: https://api.deepseek.com/v1
  api-key: <你的密钥>
  model: deepseek-chat
```

也可用环境变量覆盖：`AI_ENABLED`、`AI_API_KEY`、`AI_BASE_URL`、`AI_MODEL`。修改后需**重启后端**生效；前端 `streamAiChat()` 不传 `model` 字段，由后端 `AiChatService.buildRequestBody()` 读取 `aiProperties.getModel()`。

> **答辩补充**：架构上是后端代理 + OpenAI 兼容 `/chat/completions`，模型可插拔。当前接 DeepSeek `deepseek-chat`；若换模型，Agent 的 Prompt、上下文注入、`<plan>/<answer>` 解析逻辑均不变，仅配置项不同。

### 3.2 核心实现流程

```
用户点击右下角 FAB 机器人按钮
    │
    ▼
AiChatPanel（右侧 Sheet 抽屉面板）
    │
    ├── 1. 打开时从 localStorage 加载会话
    ├── 2. fetchAiStatus() → GET /apis/ai/status 检查 AI 是否配置
    └── 3. 用户输入消息，选择模式（普通/深度思考/Agent）
    │
    ▼
runChat(userMessage)
    │
    ├── 添加 user 消息到列表
    ├── 添加空 assistant 消息（streaming=true）
    ├── 构建 history（最近 10 条有效消息）
    │
    ├── Agent 模式：先 buildAgentContext()
    │     └── 并行拉取 dashboard + homeInfo → 格式化摘要文本
    │
    └── streamAiChat(payload, handlers, signal)
          │
          ├── fetch() POST /apis/ai/chat
          │     ├── Authorization: Bearer xxx
          │     ├── X-Workspace-Id, X-Storage-Platform-Config-Id
          │     └── body: { message, deepThink, agentMode, context, history, memory }
          │
          ├── 后端 AiChatService.streamChat()
          │     ├── 检查 ai.enabled + apiKey
          │     ├── 发送 SSE event: meta { deepThink, agentMode }
          │     ├── 构建 messages（system + history + user）
          │     ├── HttpClient POST OpenAI 兼容 /chat/completions（当前 DeepSeek）
          │     ├── 逐行读取 SSE stream
          │     └── 转发 delta/done/error 事件到前端
          │
          └── 前端处理 SSE 事件
                ├── event: meta → 记录模式信息
                ├── event: delta { text } → 累积文本，按模式解析
                │     ├── 普通模式：全文即 answer
                │     ├── 深度思考：解析 <thinking>/<answer> 标签
                │     └── Agent 模式：解析 <plan>/<answer> 标签
                ├── event: done → 流式结束，finalize 内容
                └── event: error → 标记失败，展示错误信息
    │
    ▼
用户可执行操作
    ├── 发送新消息
    ├── 停止生成（abort controller）
    ├── 重试（replaceLastPair 替换最后一轮问答）
    ├── 复制回答
    ├── 语音播报回答
    └── 语音输入（实时模式：自动听写→发送→播报→循环）
    │
    ▼
300ms 防抖后持久化到 localStorage（会话 + 消息 + 模式设置）
```

### 3.3 关键代码文件

| 文件 | 核心职责 |
|------|----------|
| `src/api/ai.ts` | SSE 流式客户端、XML 标签解析器（deep think / agent）、finalize |
| `src/components/ai-assistant/ai-chat-panel.tsx` | 完整对话面板 UI、模式切换、会话管理、语音 |
| `src/components/ai-assistant/ai-assistant-fab.tsx` | 右下角悬浮入口按钮 |
| `src/components/ai-assistant/ai-chat-storage.ts` | localStorage 多会话存储 |
| `src/hooks/use-speech-recognition.ts` | 语音识别 Hook |
| `src/hooks/use-speech-synthesis.ts` | 语音合成 Hook |
| `AiChatController.java` | 后端 SSE 端点 |
| `AiChatService.java` | LLM 代理转发 + System Prompt |

### 3.4 核心代码片段

**SSE 流式客户端（ai.ts）：**

```typescript
/**
 * AI 流式对话客户端（SSE）
 * 使用原生 fetch 而非 axios，因为需要 ReadableStream 逐块读取
 */
export async function streamAiChat(payload, handlers, signal?) {
  // 1. 发起 POST 请求，携带认证头和工作空间 ID
  const res = await fetch(resolveApiUrl('/apis/ai/chat'), {
    method: 'POST',
    headers: buildAuthHeaders(), // Bearer Token + X-Workspace-Id
    body: JSON.stringify(payload), // message / deepThink / agentMode / context / history
    signal, // AbortController，用户点击「停止生成」时中断
  })

  const reader = res.body.getReader() // 获取可读流
  const decoder = new TextDecoder()   // 二进制 → UTF-8 文本
  let buffer = ''                     // 累积未完整的 SSE 块

  /** 解析单个 SSE 事件块（格式：event: xxx\ndata: {...}） */
  const processBlock = (block: string) => {
    const lines = block.split('\n')
    let eventName = 'message'
    let dataLine = ''
    for (const line of lines) {
      if (line.startsWith('event:')) eventName = line.slice(6).trim()
      else if (line.startsWith('data:')) dataLine += line.slice(5).trim()
    }
    const data = JSON.parse(dataLine)
    // 按事件类型分发：meta(模式信息) / delta(增量文本) / done(结束) / error(错误)
    if (eventName === 'delta') handlers.onDelta?.(data.text)
    else if (eventName === 'done') handlers.onDone?.()
    // ...
  }

  // 2. 循环读取流，按 \n\n 分割完整 SSE 事件块
  while (true) {
    const { done, value } = await reader.read()
    if (done) break
    buffer += decoder.decode(value, { stream: true }) // stream:true 处理多字节字符截断
    const parts = buffer.split('\n\n')  // SSE 协议以双换行分隔事件
    buffer = parts.pop() ?? ''          // 最后一段可能不完整，留到下次拼接
    for (const part of parts) {
      if (part.trim()) processBlock(part)
    }
  }
}
```

**后端 LLM 代理（AiChatService.java）：**

```java
/**
 * 创建 SSE 连接并异步执行流式转发
 * 使用虚拟线程池，每个请求一个轻量虚拟线程，适合 IO 密集型 SSE 场景
 */
public SseEmitter streamChat(AiChatRequestDTO request) {
    SseEmitter emitter = new SseEmitter(timeoutMs); // 120s 超时
    executor.execute(() -> runStream(request, emitter)); // 虚拟线程异步执行
    return emitter; // 立即返回，HTTP 连接保持打开
}

private void runStream(AiChatRequestDTO request, SseEmitter emitter) {
    // 1. 构建 OpenAI 格式的 messages 数组
    List<Map<String, String>> messages = new ArrayList<>();
    messages.add(Map.of("role", "system", "content", buildSystemContent(request))); // 按模式选 Prompt
    // 追加历史对话（最近 10 条）+ 当前用户消息
    for (AiChatMessageDTO msg : request.getHistory()) {
        messages.add(Map.of("role", msg.getRole(), "content", msg.getContent()));
    }
    messages.add(Map.of("role", "user", "content", request.getMessage()));

    // 2. 请求上游 LLM API（OpenAI 兼容接口，stream=true）
    HttpRequest httpRequest = HttpRequest.newBuilder()
        .uri(URI.create(baseUrl + "/chat/completions"))
        .header("Authorization", "Bearer " + apiKey) // API Key 仅存服务端，前端不可见
        .header("Content-Type", "application/json")
        .POST(BodyPublishers.ofString(body)) // body 含 model、messages、stream:true
        .build();

    // 3. 逐行读取上游 SSE 响应，转发 delta 事件给前端
    try (BufferedReader reader = new BufferedReader(
            new InputStreamReader(response.body()))) {
        String line;
        while ((line = reader.readLine()) != null) {
            if (line.startsWith("data:")) {
                String data = line.substring(5).trim();
                if ("[DONE]".equals(data)) break; // 上游流结束标记
                String delta = extractDeltaContent(data); // 解析 choices[0].delta.content
                if (hasText(delta)) {
                    sendEvent(emitter, "delta", Map.of("text", delta)); // 转发增量文本
                }
            }
        }
    }
    sendEvent(emitter, "done", Map.of("ok", true)); // 通知前端流式结束
    emitter.complete(); // 关闭 SSE 连接
}
```

**三种 System Prompt 模式：**

```java
/**
 * 根据对话模式选择 System Prompt
 * buildSystemContent() 会追加 Agent 上下文到 prompt 尾部
 */

// 普通模式：直接问答，无特殊输出格式约束
private static final String NORMAL_SYSTEM = """
    你是 Free-FS 企业网盘智能助手，擅长文件管理、存储规划、权限协作与传输优化。
    用简洁、专业的中文回答；涉及操作步骤时分点说明。""";

// 深度思考模式：强制 XML 标签输出，前端分离渲染思考过程与最终答案
private static final String DEEP_THINK_SYSTEM = """
    用户已开启「深度思考」模式：
    1. 先在 <thinking> 与 </thinking> 之间逐步推理（前端琥珀色区域展示）
    2. 再在 <answer> 与 </answer> 之间给出简洁回答（前端常规区域展示）""";

// Agent 模式：注入工作空间实况后，要求结构化输出 plan + answer
private static final String AGENT_SYSTEM = """
    你是 Free-FS 网盘智能 Agent，能结合【当前工作空间实况】分析：
    1. 先在 <plan> 中列出 2～5 条执行步骤（前端紫色区域展示）
    2. 再在 <answer> 中给出结论与风险提醒（前端蓝色区域展示）
    若实况数据不足，在 plan 中说明需用户补充的信息""";
```

**前端 XML 解析器：**

```typescript
/**
 * 深度思考模式：从流式累积文本中实时解析 thinking / answer
 * 正则支持标签未闭合（流式过程中 </thinking> 可能还没到达）
 */
export function parseDeepThinkContent(raw: string) {
  // 匹配 <thinking> 到 </thinking> 或文本末尾（流式未闭合）
  const thinkingMatch = raw.match(/<thinking>([\s\S]*?)(?:<\/thinking>|$)/i)
  // 匹配 <answer> 到 </answer> 或文本末尾
  const answerMatch = raw.match(/<answer>([\s\S]*?)(?:<\/answer>|$)/i)
  // 判断是否仍在思考中：有开标签但无闭标签且无 answer 标签
  const inThinking =
    /<thinking>/i.test(raw) && !/<\/thinking>/i.test(raw) && !/<answer>/i.test(raw)

  return {
    thinking: thinkingMatch?.[1]?.trim() ?? '', // 思考过程（琥珀色区域展示）
    answer: answerMatch?.[1]?.trim() ?? '',     // 最终回答
    inThinking,                                  // true 时 UI 显示「思考中...」动画
  }
}
```

### 3.5 重难点分析

| 问题 | 解决方案 |
|------|----------|
| **流式文本逐步解析** | 使用 `parseStreamParts()` 实时解析流式累积文本，按模式提取 thinking/plan/answer |
| **流式结束时标签不完整** | `finalizeStreamParts()` 处理后处理，未闭合标签回退为全文 |
| **语音实时对话循环** | `voiceRealtime` 模式：听写 → 空格检测 → 自动发送 → 播报回复 → 重新听写 |
| **多会话管理** | `ai-chat-storage.ts` 使用 localStorage，store 扁平结构管理 sessions 列表 |
| **模型未按标签输出（容错）** | `splitAgentFallback()` 启发式拆分：按步骤列表/结论标题分段 |
| **请求取消** | `AbortController` 中断 fetch，停止生成时标记消息为已停止 |
| **SSE 连接错误处理** | HTTP 错误解析响应体提取 message，网络错误区分 TypeError 和普通 Error |
| **AI 未配置时的体验** | `fetchAiStatus()` 轮询 AI 状态，面板顶部显示未配置提示，禁用发送按钮 |

### 3.6 项目答辩话术

> AI 对话是我们网盘的智能助手功能，支持三种对话模式：普通问答、深度思考、Agent 模式。
>
> 前端使用 SSE 流式接收 LLM 输出，逐块累积文本并根据当前模式实时解析。普通模式全文即回答；深度思考模式下，模型先输出 `<thinking>` 标签内的推理过程，再输出 `<answer>` 标签内的结论；Agent 模式则是先规划步骤再给出建议。
>
> 后端只做代理转发——接收前端请求，调用 OpenAI 兼容的 API，通过 SseEmitter 把流式输出转发给前端。这样设计的好处是 AI 配置集中在后端，可以随时切换模型，前端无感知。
>
> 我们还支持语音输入和语音播报、多会话管理（localStorage 持久化）、失败重试等特性，让 AI 交互更自然。

---

## 4. Agent 智能助手

> **模型说明**：Agent 不单独指定大模型，与普通对话、深度思考共用 §3.1.1 中的 DeepSeek `deepseek-chat` 配置；差异仅在 `AGENT_SYSTEM` Prompt 与工作空间上下文注入。

### 4.1 核心技术栈

| 技术 | 是什么 | 为什么用 | 如何使用 |
|------|--------|----------|----------|
| **Prompt Engineering** | 通过提示词引导 LLM 行为 | 无需复杂的 Tool-Calling 框架，实现轻量 Agent | 后端 `AGENT_SYSTEM` 约束 LLM 输出 `<plan>`+`<answer>` 标签格式 |
| **上下文增强** | 将实时数据注入 Prompt | 让 LLM 基于真实数据而非猜测来给出建议 | 前端 `buildAgentContext()` 并行拉取 dashboard + homeInfo，后端拼入 system prompt |
| **统一流式解析** | 按模式分发解析器 | Agent/深度思考/普通三种模式共用一套 SSE 管道 | `parseStreamParts()` 流式解析，`finalizeStreamParts()` 流结束整理 |
| **XML 标签解析 + 启发式回退** | 正则匹配 + 文本分段 | 模型不按标签输出时 UI 仍能分区展示 | `parseAgentContent()` → `finalizeAgentContent()` → `splitAgentFallback()` |

### 4.2 核心实现流程

```
用户切换到 Agent 模式 → 输入问题 或 点击快捷提示词
    │
    ▼
1. buildAgentContext() ── 并行请求
    ├── getDashboard({ unit: 2 })  → 已用存储、文件数、分享数、回收站等
    └── getHomeInfo({ unit: 2, dateType: 2 }) → 7 日趋势、最近文件
    │
    ▼
2. 格式化为纯文本摘要：
    已用存储：2.3 GB
    文件数：1,247，文件夹：89
    分享数：12，回收站：35
    今日上传：6 个
    近 7 日趋势：[...]
    最近文件：[...]
    │
    ▼
3. streamAiChat({
    message: userQuestion,
    agentMode: true,
    context: "【当前工作空间实况】\n已用存储：2.3 GB\n..."
  })
    │
    ▼
4. 后端构建 AGENT_SYSTEM + context → 发送给 LLM
    │
    ▼
5. LLM 输出：
    <plan>
    1. 进入回收站筛选 30 天前的文件
    2. 确认无重要文件后批量清空
    3. 返回首页验证存储用量是否下降
    </plan>
    <answer>
    建议先备份后再清理。清理后预计可释放约 500MB 空间。
    </answer>
    │
    ▼
6. 前端流式解析（onDelta 回调）
    ├── acc += chunk 累积文本
    ├── parseStreamParts(acc, { agentMode: true }) → plan / answer / inProgress
    └── plan 区块紫色边框，answer 区块常规展示；inPlan 时显示 Planning...
    │
    ▼
7. 流结束 finalizeStreamParts() → finalizeAgentContent()
    ├── 有 XML 标签 → 直接提取 plan / answer
    └── 无标签 → splitAgentFallback() 启发式拆分
    │
    ▼
8. Agent 仅提供「建议」，不自动执行操作（无 Tool-Calling）
```

### 4.3 关键代码文件

| 文件 | 核心职责 |
|------|----------|
| `src/components/ai-assistant/ai-agent-context.ts` | `buildAgentContext()` 并行拉取 dashboard/home，格式化工作空间实况 |
| `src/components/ai-assistant/ai-chat-panel.tsx` | Agent 模式切换、`runChat()` 拉取上下文 + 流式解析 + 紫色/蓝色分区 UI |
| `src/api/ai.ts` | `parseAgentContent()` / `finalizeAgentContent()` / `splitAgentFallback()` / `parseStreamParts()` |
| `AiChatService.java` | `AGENT_SYSTEM` 常量 + `buildSystemContent()` 将 context 拼入 system prompt |

### 4.4 核心代码片段

**前端：上下文构建 → 发送 → 解析（3 步闭环）**

```typescript
// 1. ai-agent-context.ts — 并行拉取工作空间数据
export async function buildAgentContext() {
  const [dash, home] = await Promise.all([
    getDashboard({ unit: 2 }),
    getHomeInfo({ unit: 2, dateType: 2 }),
  ])
  const lines = [
    `已用存储：${dash.usedStorage} ${dash.unit}`,
    `文件数：${dash.fileCount}，文件夹：${dash.folderCount}`,
    // + 分享数、回收站、7 日趋势、最近文件...
  ]
  return { text: lines.join('\n'), loaded: lines.length > 0 }
}

// 2. ai-chat-panel.tsx — 拉上下文 + 流式对话
const ctx = agentMode ? await buildAgentContext() : { text: '', loaded: true }
await streamAiChat({ message: text, agentMode, context: ctx.text, history }, {
  onDelta: (chunk) => {
    acc += chunk
    const { plan, answer } = parseStreamParts(acc, { agentMode, deepThink: false })
    patchAssistant({ plan, answer, streaming: true })
  },
  onDone: () => {
    const { plan, answer } = finalizeStreamParts(acc, { agentMode, deepThink: false })
    patchAssistant({ plan, answer, streaming: false })
  },
})

// 3. ai.ts — 解析 <plan>/<answer>，无标签时 splitAgentFallback() 启发式拆分
export function parseAgentContent(raw: string) {
  const plan = raw.match(/<plan>([\s\S]*?)(?:<\/plan>|$)/i)?.[1]?.trim() ?? ''
  const answer = raw.match(/<answer>([\s\S]*?)(?:<\/answer>|$)/i)?.[1]?.trim() ?? ''
  return { plan, answer }
}
export function finalizeAgentContent(raw: string) {
  const parsed = parseAgentContent(raw)
  return (parsed.plan || parsed.answer) ? parsed : splitAgentFallback(raw)
}
```

**后端：System Prompt + 上下文注入**

```java
// AiChatService.java
private String buildSystemContent(AiChatRequestDTO request) {
    String base = Boolean.TRUE.equals(request.getAgentMode()) ? AGENT_SYSTEM : NORMAL_SYSTEM;
    if (Boolean.TRUE.equals(request.getAgentMode()) && StringUtils.hasText(request.getContext())) {
        return base + "\n\n【当前工作空间实况】\n" + request.getContext().trim();
    }
    return base;
}

private static final String AGENT_SYSTEM = """
    你是 Free-FS 网盘智能 Agent，结合【当前工作空间实况】给出可执行方案。
    1. 在 <plan> 中列出 2～5 条执行步骤
    2. 在 <answer> 中给出结论与风险提醒""";
```

### 4.5 重难点分析

| 问题 | 解决方案 |
|------|----------|
| **LLM 未按 XML 标签输出** | `splitAgentFallback()` 三级回退：①结论标题分段 → ②步骤编号切分 → ③多步骤附默认引导语 |
| **工作空间数据不足** | `contextEmpty` toast 提示 Agent 可能缺乏足够数据，仍允许对话 |
| **Agent 上下文加载状态** | `loadingContext` 状态，加载时禁用输入并显示提示 |
| **Agent 与 DeepThink 互斥** | Agent 模式强制关闭 DeepThink，前后端都做校验 |
| **Agent 无 Tool-Calling 能力** | Agent 只提供建议性步骤，不自动执行或调用 API |

### 4.6 项目答辩话术

> Agent 智能助手是基于 Prompt Engineering 的轻量级实现。它不同于传统的 Tool-Calling 架构——我们没有给 LLM 挂载真实的 API 调用能力，而是通过上下文增强让 LLM 了解当前工作空间的真实状态。
>
> 具体来说，用户开启 Agent 模式后，前端会先并行拉取仪表盘和首页数据，包括存储用量、文件数、分享数、回收站等，格式化成纯文本摘要，作为"当前工作空间实况"注入到 Prompt 中。LLM 根据这些真实数据来分析问题、给出可执行的步骤建议。
>
> 输出格式是 `<plan>` 步骤列表 + `<answer>` 结论建议，前端解析后分紫色和蓝色两个区块展示。如果 LLM 没有按标签格式输出，我们还有启发式回退算法自动拆分。

---

## 5. CI/CD 流水线

### 5.1 核心技术栈

| 技术 | 是什么 | 为什么用 | 如何使用 |
|------|--------|----------|----------|
| **GitHub Actions** | GitHub 内置 CI/CD 服务 | 代码仓库和 CI/CD 在同一平台，无需外部集成 | `.github/workflows/ci.yml`、`deploy.yml`、`cd.yml` |
| **Maven** | Java 构建工具 | 标准化 Java 项目构建 | `mvn -B clean package -DskipTests` 打包 JAR |
| **pnpm** | 快速、节省磁盘的包管理器 | 比 npm/yarn 更快，硬链接节省磁盘 | `pnpm install --frozen-lockfile`，`pnpm build` |
| **PM2** | Node 进程管理工具 | 虽管理 Java 进程，但 PM2 的 keep-alive/日志/重启很方便 | `pm2 startOrReload ecosystem.config.cjs` |
| **Docker / Compose** | 容器化技术 | 环境一致性、快速部署 | 手动 CD 构建镜像，compose 编排 MySQL+Redis+后端 |
| **SSH + SCP** | 远程连接与文件传输 | 将构建产物传输到服务器 | GitHub Actions SSH Action + deploy.sh 脚本 |

### 5.2 核心实现流程

```
GitHub Actions CI/CD Pipeline
═══════════════════════════════

CI（持续集成）— push/PR 自动触发
┌─────────────────────────────────────────┐
│  ci.yml                                 │
│                                        │
│  trigger: push (main/develop/feature)   │
│           pull_request (main/develop)   │
│                                        │
│  backend:                               │
│    → Set up JDK 21                     │
│    → mvn -B clean package -DskipTests  │
│    → Upload fs-admin.jar artifact      │
│                                        │
│  frontend:                              │
│    → pnpm install --frozen-lockfile     │
│    → pnpm lint (continue-on-error)     │
│    → pnpm build                        │
│    → Upload dist artifact              │
└─────────────────────────────────────────┘

Deploy（自动部署）— push main 触发
┌─────────────────────────────────────────┐
│  deploy.yml                             │
│                                        │
│  trigger: push main                    │
│                                        │
│  steps:                                 │
│  1. Build frontend (pnpm build)         │
│  2. Build backend (mvn package)         │
│  3. Prepare deploy bundle:              │
│       deploy-output/                    │
│       ├── frontend/   ← dist/*          │
│       ├── backend/    ← fs-admin.jar    │
│       └── deploy/     ← deploy.sh +     │
│                         ecosystem.cjs   │
│  4. SCP to server:                      │
│       $DEPLOY_PATH/release/             │
│  5. SSH: bash deploy.sh                │
└─────────────────────────────────────────┘

CD（手动 Docker 部署）— workflow_dispatch
┌─────────────────────────────────────────┐
│  cd.yml                                 │
│                                        │
│  trigger: workflow_dispatch             │
│    inputs: push_image (true/false)      │
│                                        │
│  steps:                                 │
│  1. mvn -B clean package -DskipTests   │
│  2. docker build -f docker/Dockerfile   │
│     tags: free-fs:${{ sha }}, latest    │
│  3. [optional] docker push             │
│  4. [optional] docker-compose up       │
└─────────────────────────────────────────┘

服务器端部署脚本 (deploy.sh)
┌─────────────────────────────────────────┐
│  1. rsync frontend/ → $APP_ROOT/frontend│
│  2. cp backend/fs-admin.jar → backend/  │
│  3. sed 替换 PM2 配置中的占位符         │
│  4. pm2 startOrReload ecosystem.cjs     │
└─────────────────────────────────────────┘
```

### 5.3 关键文件

| 文件 | 核心职责 |
|------|----------|
| `.github/workflows/ci.yml` | push/PR 构建验证 |
| `.github/workflows/deploy.yml` | push main 自动部署到服务器 |
| `.github/workflows/cd.yml` | 手动触发 Docker 构建 |
| `deploy/deploy.sh` | 服务器端部署脚本（rsync + PM2） |
| `deploy/ecosystem.config.cjs` | PM2 进程管理配置 |
| `free-fs-master/docker/Dockerfile` | 后端容器镜像 |
| `free-fs-master/docker/docker-compose.yml` | MySQL + Redis + 后端编排 |

### 5.4 部署模式对比

| 维度 | SSH + PM2（deploy.yml） | Docker（cd.yml） |
|------|------------------------|-----------------|
| 触发方式 | push main 自动 | 手动 workflow_dispatch |
| 前端部署 | rsync 静态文件 | 不在镜像内，需额外托管 |
| 后端部署 | PM2 管理 JAR 进程 | Docker 容器运行 |
| 数据库 | 服务器预装 MySQL/Redis | Compose 自带 |
| 适用场景 | 轻量快速部署 | 容器化环境 |

### 5.5 重难点分析

| 问题 | 解决方案 |
|------|----------|
| **SSH 私钥认证** | GitHub Secrets 存储私钥，部署步骤中加载 SSH 私钥文件 |
| **前端 API 地址注入** | Vite 构建时通过 `VITE_API_BASE_URL` 环境变量注入 |
| **PM2 配置模板化** | `ecosystem.config.cjs` 使用 `__APP_ROOT__` 占位符，部署时 sed 替换 |
| **JAR 包安全替换** | 先上传到 release 目录，再 cp 到运行目录，避免直接覆盖运行中文件 |
| **Docker 镜像版本** | 同时打 `:latest` 和 `:${{ github.sha }}` 两个 tag |

### 5.6 项目答辩话术

> 我们的 CI/CD 流水线有三条：`ci.yml` 做构建验证，每次 push 和 PR 都会自动编译后端和前端，保证代码质量；`deploy.yml` 做自动部署，当代码合并到 main 分支时，自动构建前后端，打包后通过 SCP 传到服务器，再 SSH 执行部署脚本（rsync 前端 + 复制 JAR + PM2 重启）；`cd.yml` 是做 Docker 部署的，手动触发，适合容器化环境。
>
> 生产环境我们用的是 PM2 + SSH 模式——前端直接 rsync 静态文件，后端用 PM2 管理 JAR 进程，自带 keep-alive、日志轮转和内存限制，非常稳定。

---

## 综合面试准备

### 项目总体介绍话术

> Free-FS 是一个企业级文件管理系统，采用前后端分离架构。前端基于 React 19 + TypeScript + Vite，后端基于 Spring Boot 4 + JDK 21 + MyBatis-Flex。核心功能包括大文件分片上传、数据可视化大屏、AI 智能对话、Agent 助手，以及完整的 CI/CD 自动化部署。

### 项目亮点总结

| 亮点 | 一句话说明 |
|------|------------|
| 大文件上传 | 分片上传 + 采样秒传 + 断点续传 + 智能进度计算 |
| 数据大屏 | HTTP 三路轮询（指标 30s / 图表 60s）+ Recharts 面积图 + 深色科技风 |
| AI 对话 | SSE 流式 + 三种 Prompt 模式 + 语音交互 |
| Agent 助手 | 上下文增强 + XML 结构化输出 + 启发式回退 |
| CI/CD | 三条 Actions 流水线 + SSH 自动部署 + Docker 可选 |